[project]
name = "sdlc-worker-orchestrator"
version = "0.1.0"
description = "SDLC orchestrator worker — Analyst, Coder, Reviewer agents"
requires-python = ">=3.13"
dependencies = [
    "aiohttp>=3.11.0",
    "azure-identity>=1.25.3",
    "azure-servicebus>=7.14.2",
]
